namespace my_stl
{
    int str_to_hash(const char* str);
    int two_str_to_hash(const char* str1, const char* str2);
    int two_str_to_hash_sort(const char* str1, const char* str2);

    int count_num(int n);

    int abs(int n);
    int pow(int n, int exp);
    int max(int a, int b);
    int min(int a, int b);
}